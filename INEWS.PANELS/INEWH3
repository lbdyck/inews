)Attr Default(%+_)
/* + type(text  ) intens(low )                   information only  */
/* _ type( input) intens(high) caps(on ) just(left )               */
   ! type(text) intens(high) caps(off) just(asis ) color(white)
   ` type(text) intens(high) caps(off) just(asis ) color(blue)
   ~ type(text) intens(high) caps(off) just(asis ) color(turq)
   % type(text) intens(high)
   ^ type(text) intens(high) caps(off) just(asis ) color(turq)
   # type(output) intens(high) color(turq)
)Body
+----------------------% News and Information Tutorial +------------------------
%Selection ===>_zcmd
%
~                           Table Status Values
~
~  Value      Explanation
~
`  Ignored   ~You have chosen to mark the item as Ignored.
`  New       ~This is a New item that you have never seen before.
`  Read      ~You have read the item
`  Unread    ~You have not Read the item (or you reset the status to Unread)
             ~and you have seen the item in the table display.
~
`  Expired   ~The item has an expiration date which has expired.
`  NotAppl   ~The item is for another center and does not apply to this
             ~center.

~  * Expired only applies if the author established an expiration date.

~  * NotAppl only applies if the author restricted this item to specific
     center.
)Init
)Proc
 &zcont = inewh0
)End
